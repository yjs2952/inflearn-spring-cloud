server:
  port: 8888

spring:
  application:
    name: config-service
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///D:\STUDY\repository\inflearn-spring-cloud-part3
#          search-locations: file://${user.home}/Desktop/dev/workspace/inflearn-spring-cloud-data
        git:
#          uri: file://D:\STUDY\repository\inflearn-spring-cloud-part2
          uri: https://github.com/yjs2952/spring-cloud-config
#          username:  # private 인 경우
#          password:  # private 인 경우
management:
  endpoint:
    web:
      exposure:
        include: health, busrefresh